services:
  postgres:
    image: postgres:16
    container_name: novatra_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: novatra
    ports:
      - "5432:5432"
    volumes:
      - novatra_pgdata:/var/lib/postgresql/data

volumes:
  novatra_pgdata:

