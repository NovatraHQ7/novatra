# `packages/stellar`

Stellar settlement + transaction building layer.

Planned contents:

- build/sign/submit payments on Stellar
- memo formats, trustline helpers
- corridor configuration (issuer, asset, anchor endpoints)

## Environment

- `NOVATRA_STELLAR_NETWORK` = `testnet` (default) | `public`
- `NOVATRA_HORIZON_URL` (optional)
